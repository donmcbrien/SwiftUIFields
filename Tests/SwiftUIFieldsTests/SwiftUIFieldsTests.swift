    import XCTest
    @testable import SwiftUIFields

    final class SwiftUIFieldsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftUIFields().text, "Hello, World!")
        }
    }
